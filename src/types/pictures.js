// @flow
export type Picture = {
  id: string,
  cropped_picture: string,
};

export type PictureDetails = {
  id: string,
  author: string,
  camera: string,
  cropped_picture: string,
  full_picture: string,
};
